git clone git@github.com:Qodestackr/JamiiHub.git 

or git clone https://github.com/Qodestackr/JamiiHub.git

cd into the repo

`yarn` or `npm install`

To start the project: `yarn dev` or `npm run dev`